# ==============================================
# FOR WINDOWS (MT5)
# ==============================================
# Example .env for MT5 live trading (Windows PowerShell)
# Replace placeholders with your actual values

MT5_TERMINAL_PATH=C:\\Program Files\\MetaTrader 5\\terminal64.exe
MT5_LOGIN=12345678
MT5_PASSWORD=your_mt5_password
MT5_SERVER=YourBroker-Server

# Optional:
MT5_SYMBOL=USDJPY

# ==============================================
# ATR-BASED VOLATILITY MANAGEMENT
# ==============================================
# Professional adaptive risk system:
# - ATR(14) calculates market volatility
# - SL/TP scale with volatility (maintains R:R ≈ 1:1.6)
# - Lot size inversely scales (fixed % risk per trade)
# - Extreme volatility protection (pause/reduce size)

ATR_VOLATILITY_ADJUSTMENT=true
ATR_PERIOD=14
ATR_BASELINE_PIPS=30  # Baseline ATR for normal conditions

# Stop Loss / Take Profit Limits
MIN_SL_PIPS=20  # Minimum SL (calm markets)
MAX_SL_PIPS=80  # Maximum SL (volatile markets)
RISK_REWARD_RATIO=1.6  # TP = SL * 1.6

# Fixed % Risk per Trade
RISK_PERCENT_PER_TRADE=0.5  # 0.5% of balance per trade
PIP_VALUE_PER_LOT=9.17  # USDJPY: $9.17/pip/lot @ 1.0

# Position Sizing Limits
MIN_LOT_SIZE=0.10  # Broker minimum
MAX_LOT_SIZE=1.50  # Maximum per trade
MAX_TOTAL_LOTS=3.0  # Maximum across all positions

# Extreme Volatility Protection
EXTREME_VOL_FACTOR=2.5  # Skip if ATR > 2.5x baseline
HIGH_VOL_FACTOR=2.0  # Halve size if ATR > 2.0x

# Position Management
MAX_POSITIONS=3
MIN_INTERVAL_SECONDS=900  # 15 min spacing

# Daily Loss Limits (FTMO-safe)
MAX_DAILY_LOSS=4948  # Hard: $4,948 (5% of $100k)
DAILY_LOSS_SOFT_STOP_PCT=3.0  # Soft: 3% drawdown

# ==============================================
# SIGNAL THRESHOLDS
# ==============================================
# Lower thresholds = more trades (FTMO requires consistency)
# Probability thresholds for trading signals
BUY_THRESHOLD=0.70
SELL_THRESHOLD=0.30

# Minimum probability difference to consider signal strong
MIN_PROBABILITY_DIFF=0.15

# ==============================================
# FOR UBUNTU/LINUX (Oanda API)
# ==============================================
# Use this configuration for Azure VM "Sabyr"

# Oanda API Configuration
OANDA_API_TOKEN=your_api_token_here
OANDA_ACCOUNT_ID=your_account_id_here
OANDA_ENVIRONMENT=practice

# Symbol to trade
SYMBOL=USDJPY

# ==============================================
# Get Oanda API Token:
# 1. Login: https://www.oanda.com/demo-account/login
# 2. Click: Manage API Access
# 3. Generate: Personal Access Token
# 4. Copy: Token and Account ID
# ==============================================

# For security: do NOT commit your real .env to git
